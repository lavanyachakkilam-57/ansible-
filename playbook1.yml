---
- name: create a user on all servers
  hosts: all
  become: yes
  tasks:
    - name: user creation
      user:
        name: lucky
        password: lucky
        uid: 8899
        home: /tmp/lucky
        shell: /bin/bash
        comment: "a normal user"
...
